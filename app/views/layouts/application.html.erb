<!DOCTYPE html>
<html>
  <head>
    <title>Calculation</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="flex gap-4">
      <aside class="flex flex-col justify-between pb-4 w-64 bg-slate-900 min-h-screen">
        <div class="text-white">
          <h1 class="text-2xl font-bold my-6 text-center">Calculation</h1>
          <ul>
            <li class="flex"><%= link_to 'Dashboard', root_path, class: "w-full py-4 hover:bg-slate-800 px-6" %></li>
            <li class="flex"><%= link_to 'Kinda Another Menu', root_path, class: "w-full py-4 hover:bg-slate-800 px-6" %></li>
          </ul>
        </div>

        <div class="px-4">
          <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn-danger w-full" %>
        </div>
      </aside>

      <div class="px-6 pt-8">
        <%= yield %>
      </div>    
    </main>
  </body>
</html>
